# LXC Deck 生产环境变量
# 必改：随机长字符串（节点凭据加密 + JWT 签名密钥）
LXCP_SECRET=change-me-to-a-long-random-string
# 面板对外地址（生成 Agent 安装/卸载命令、订阅链接用）
LXCP_PUBLIC_BASE=https://panel.example.com
# 监听端口
LXCP_PORT=8080
# SQLite 路径（Docker 内使用 /data/panel.db）
LXCP_DB=./data/panel.db
